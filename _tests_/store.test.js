
import configureStore from 'redux-mock-store'
const mockStore = configureStore([]);

describe('My Connected React-Redux Component', () => {
  let store; 

  beforeEach (() => {
    store = mockStore({
      myState: 'sample text', 
    });
  })

  component = 

  if('should render with given state from Redux store', () => {
    
  });

})